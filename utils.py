#===========================
# keyword generator
#===========================

def get_keywordlist(title, url, highlight, memo, other_tags):
    htmlParsing(url)
    return keywords

def htmlParsing(url): #return nouns from url
    return nouns
